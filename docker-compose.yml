services:
  mongo:
    image: mongo:7.0
    container_name: quiz-mongo
    ports:
      - '27017:27017'
    environment:
      MONGO_INITDB_DATABASE: quiz
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
