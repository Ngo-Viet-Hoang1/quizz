{
  "$schema": "https://turbo.build/schema.json",
  "remoteCache": { "enabled": true },
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },

    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    },

    "lint": {
      "outputs": []
    },

    "format": {
      "outputs": []
    },

    "test": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "test/**", "jest.config.*", "tsconfig*.json"],
      "outputs": ["coverage/**"]
    },

    "codegen": {
      "dependsOn": ["^build"],
      "outputs": ["packages/api-client/**"]
    },

    "dev": {
      "cache": false,
      "persistent": true
    },

    "clean": {
      "cache": false
    }
  }
}
